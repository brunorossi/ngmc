{{!log this}}
<!DOCTYPE html>
<html>
	<head>
		{{>head}}
	</head>
	<body class="{{!body_class}}">
		{{>navigation}}
		<section id="wrapper">
			<a class="hidden-close"></a>
			{{>post-header}}
			{{>post-content}}
			{{>footer}}
		</section>
	</body>
</html>
